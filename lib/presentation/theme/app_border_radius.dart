import 'package:flutter/cupertino.dart';

class AppBorderRadius {
  AppBorderRadius._();

  static const borderAll6 = BorderRadius.all(Radius.circular(6));
  static const borderAll10 = BorderRadius.all(Radius.circular(10));
  static const borderAll12 = BorderRadius.all(Radius.circular(12));
  static const borderAll16 = BorderRadius.all(Radius.circular(16));
  static const borderAll30 = BorderRadius.all(Radius.circular(30));
  static const max = BorderRadius.all(Radius.circular(10000));
}
